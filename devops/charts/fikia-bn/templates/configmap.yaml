apiVersion: v1
data:
  {{- range $key, $value := .Values.env_vars }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
kind: ConfigMap
metadata:
  name: {{ include "fikia-bn.fullname" . }}
  labels:
    {{- include "fikia-bn.labels" . | nindent 4 }}
